import React, { memo } from 'react';
import '../index.less';

const ParticipantLaneSet = (props) => {
  return (
    <div className='laneSet'>
    </div>
  )
}

export default memo(ParticipantLaneSet);